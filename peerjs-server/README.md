
# PeerJS Server

This is a simple PeerJS signaling server for WebRTC applications.

## Deployment (Render)
- Build command: `npm install`
- Start command: `npm start`
- Port: `3000`

## Local Run
```bash
npm install
npm start
```
